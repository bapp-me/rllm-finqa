# 背景
1. 这个项目无法在我的本地机器上运行，只能在另一台训练机器上跑。但是代码都是一样的
2. 现在我已经做了3个实验，使用3种不同训练方法，分别产出了3个不同的checkpoint文件夹，保存的目录是：rllm/checkpoints/finqa-grpo-curriculum/，下面有3个不同的实验目录：
   1. finqa-grpo-incur
   2. finqa-grpo-neg
   3. finqa-grpo-nopen
3. 你可以认为，每个实验目录下的最新的global_step_xx文件，里面存的模型，就是当前实验最好的模型
4. 同时，我还把rllm官方训练好的模型下载到训练机器上了。是从huggingface上下载的，模型的文件路径为：rllm/finqa4b
5. 之前都是用doubao来给reward，做judge model。现在我打算换我训练机器上的模型来打分，如何访问和使用，可以参考rllm/projects/finqa/scripts/generate_negative_samples.py。
6. 我的训练机器有8张卡，我把裁判模型部署到0,1,2,3号卡上了

# 要求
## 测试集构建
我想要在测试集上，测这4个模型的效果。但是我不打算用单表和多表全量的test_finqa.csv,而是从中筛选一些出来。筛选规则如下：
1. 单表的test，只从中随机筛选250条数据
2. 多表的test，只从中随机筛选50条数据，其中medium20条，hard30条
3. 单表和多表的test_negative，则是全量加入测试集中
## 模型测评
1. 因为0,1,2,3号卡是裁判模型占用，所以在测评时，测评模型应该加载在剩下的4张卡上
2. 评测的结果，我也要log，能在swanlab上看到指标
3. log能观测的指标，和用val验证集进行验证时，能观测到的指标，要完全一致。
4. 同时我还发现，由于val时的数据集里，没有negative负样本，因此所有negative相关指标都是0。但是我没看到任何与negative相关的reward指标，不知道是0的原因，还是原本就没写？你需要检查一下，如果没写就要加上。
5. 所有4个模型，我都要进行测评
## 额外要求
除了测评的指标我要看，每个模型在测评集上的表现我也要看。把模型的rollout/infer结果，保存到一个文件里，包括所有输入输出(prompt/回答/工具返回结果等等)，以及每条结果的各项指标：
1. reward情况。单表就是总分；多表就是总分，以及各个细分规则的分数
2. 其余各项val时能看到的指标，包括但不限于步数/工具调用数量、工具调用成功率、访问表的正确情况等等。
3. 在每条结果的最前面，插入一个字段，用于表示该条数据是单表还是多表，以及是否负样本。